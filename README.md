# JS-Core-Projects

Welcome to my core JavaScript development sandbox. This repository tracks my journey from foundational programming paradigms, DOM architecture, and browser event models up to handling high-throughput asynchronous operations and remote API fault tolerance.

---

## 🚀 Featured Capstone: Tech Stack Manager (Full-CRUD Remote Engine)
The crown jewel of this workbench is an enterprise-pattern **Tech Stack Manager** that shifts application state manipulation from the local client sandbox over to live global server networks.



### 🛠️ Technical Highlights & Implementation
* **Asynchronous Network Lifecycle:** Engineered utilizing modern `async/await` syntax to orchestrate high-latency `GET`, `POST`, `PUT`, and `DELETE` requests interacting with a remote REST API.
* **Network Fault Tolerance & Thread Safety:** Bypassed blocking browser UI architectures (like `alert()`) by building a custom, asynchronous **Toast Notification Engine** that intercepts failures within `try...catch` blocks to protect user data integrity.
* **Universal Real-Time Search:** Implemented linear string filtration algorithms mapping input nodes against current global state arrays with dynamic DOM reflows.

---

## 📂 Core Laboratory Workbenches

### 1. Small Challenges Laboratory
* **Dynamic Color Palette Generator:** Translates visual DOM hex streams (`<input type="color">`) into sanitized variables via string transformations (`.replace()`) to supply remote network query strings.
* **Asynchronous Dog Image Engine:** A focused module practicing targeted DOM attribute mutations under fluctuating API response timelines.
* **Continuous Background Loop Engine:** Leveraged Web APIs outside the single-threaded JavaScript call stack using precise interval clocks.

### 2. Utility Engines
* **Binary-to-Decimal Converter (`bin2Dec`):** Algorithmic implementation evaluating exponential calculations to handle numerical base transformations.
* **Interactive Calculator:** Advanced UI layout managing manual token inputs, string buffers, and basic mathematical mutations.

---

## 📈 Key Engineering Competencies Demonstrated
* **Serialization Protocols:** Flat-packing high-level runtime memory objects into universal data structures via `JSON.stringify()` and reconstructing inputs back with `JSON.parse()`.
* **HTTP Protocol Handshakes:** Custom configuration of option blocks mapping headers (`Content-type: application/json; charset=UTF-8`) to dictate payload handling rules for servers.
* **State Syncing Strategies:** Synchronizing client-side array states only upon verification of status flags (`response.ok`) from database pipelines.

---
💡 *Maintained and continuously expanded by [Your Name]*
